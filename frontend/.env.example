# Copy to frontend/.env.local for local dev. Vite exposes only VITE_-prefixed vars.
# Points the frontend at the local backend (docker compose up) by default.
VITE_API_BASE_URL=http://localhost:8000/api/v1
